apiVersion: v1
kind: Service
metadata:
  name: {{ include "mi_app_webb.fullname" . }}
  labels:
    {{- include "mi_app_webb.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "mi_app_webb.selectorLabels" . | nindent 4 }}
