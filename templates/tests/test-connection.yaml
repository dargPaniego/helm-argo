apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "mi_app_webb.fullname" . }}-test-connection"
  labels:
    {{- include "mi_app_webb.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "mi_app_webb.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
